{
  "name": "gr-programming-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "Shop-floor machining and CNC programming calculators",
  "scripts": {
    "check": "node --check js/data.js && node --check js/core.js && node --check js/workflow.js && node --check js/app.js && node --check service-worker.js && node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  }
}
